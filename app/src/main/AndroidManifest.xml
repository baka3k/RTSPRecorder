<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecorderIJKPlayerSample"
        android:usesCleartextTraffic="true">
        <activity
            android:name="com.baka3k.stream.player.sample.VideoFilterActivity"
            android:exported="false">

        </activity>
        <activity
            android:name="com.baka3k.stream.player.sample.VideoActivity"
            android:exported="false" />
        <activity
            android:name="com.baka3k.recorderijkplayersample.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>